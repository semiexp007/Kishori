<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uietsocial.kishori">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FirebaseData"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KISHORI">
        <activity android:name=".showingStudentsProfile"></activity>
        <activity android:name=".postActivity" />
        <activity
            android:name=".s2"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".FacultyHome" />
        <activity android:name=".Chat.ChatActivity" />
        <activity android:name=".SetProfilePic" />
        <activity android:name=".Searchadd" />
        <activity android:name=".Home" />
        <activity android:name=".CreateUsers" />
        <activity android:name=".AdminPanel" />
        <activity android:name=".studentLogin" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".Notification.MyFirebaseMessagingService"
            android:directBootAware="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>